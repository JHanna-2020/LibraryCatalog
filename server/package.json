{
  "name": "library-catalog-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API + SQLite database for the personal library catalog. Runs on your always-on laptop.",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "dev": "node --env-file-if-exists=.env --watch src/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
